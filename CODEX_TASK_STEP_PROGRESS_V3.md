# Codex Task: ステップ＆ゴール進捗管理 V3 — D1直保存・高速自動保存

## 目的
現行「ステップ＆ゴール進捗管理」は、画面側に自動保存キューがある一方、ブラウザの主要な保存がGoogle Apps Scriptへ中継され、D1ミラー・二重書込み・同期処理・書込みガードが混在している。

今回は延命修正ではなく、**V3を別系統で作り、Cloudflare D1を正本として直接保存**する。

オーナー要望：
- 宿題・進捗・目標範囲等は、Googleシートへ直接入力する感覚で、原則保存ボタン不要。
- 保存ボタンを残す場合でも、通常操作は2秒以内を目標にする。
- 現在データは「おおむね」移植できればよい。少々の漏れ・ズレの完全照合に時間を使わない。
- **1320 / kase のダミー生徒で実機相当テスト**を行う。
- 過剰な全件照合・大量の防御的テスト・半日規模の検証は禁止。オーナー指定の短時間枠を設計条件として、必要最低限で切替可能な状態を優先する。

## 現行で確認済みの構造
- 現行 `index.html` には `unitQueues` / `progressBatchTimer` / 800ms debounce の自動保存UIがすでにある。
- 本番系ブランチ `agent/cloudflare-progress-migration` の `cloudflare/src/index.ts` では、`handleBrowserRpc` の主要ブラウザ read/write が `proxyGoogleRpc()` に流れている。
- `cloudflare/wrangler.jsonc` には `DUAL_WRITE_ENABLED`, `MIRROR_READ_ENABLED`, `GOOGLE_DUAL_WRITE_URL`, `GOOGLE_API_URL`, cron同期, write approval 等が同居。
- よって今回はフロントの保存ボタン修理ではなく、**保存経路そのものの単純化**が主目的。

## 作業ブランチ
このファイルが置かれている `agent/step-progress-v3-fast-autosave` で作業する。

現行本番を直接上書きしないこと。

## V3 必須アーキテクチャ
1. **新Worker / 新D1を別系統で作る**。
   - 例: Worker `step-progress-v3-staging`
   - 例: D1 `step-progress-v3-db`
2. 正本はD1のみ。少なくとも以下の利用者入力はGoogleを保存経路に入れない。
   - 進捗（単元・回・point/warmup/TRY/Exercise/LCT/学習日等、現行項目）
   - 宿題（対象、期限、完了、訂正、復習、アーカイブ/復元等）
   - 目標範囲
   - 同じ画面で利用者が変更するその他の項目も原則D1へ統一
3. Google Apps Script / Google Sheetは、必要なら初期データ取得・生徒マスタ等の参照元として残してよいが、**通常の保存成功判定をGoogle応答待ちにしない**。
4. Googleへのバックアップが必要なら、D1保存成功後の非同期処理に限定。失敗しても画面上の保存成功を取り消さない。初期切替ではGoogle二重書込み自体を省略してよい。
5. 認証はログイン時だけ既存Google認証を使ってよい。可能ならログイン成功後にV3用ローカルセッションを発行し、各保存ごとにGoogle `getSession` を呼ばない。
6. D1書込みは1操作1往復を基本とし、同時変更は `DB.batch()` でまとめる。保存後に必要な最小行のみ返す。保存直後の全ダッシュボード再取得は禁止。

## 自動保存UI
- checkbox / radio / select / date / LCTボタン等の変更で即座に画面へ反映する（optimistic UI）。
- 250〜400ms程度の短い debounce で自動保存。連続変更は1リクエストへまとめる。
- 状態表示は簡潔に `保存中…` → `保存済み`。
- 失敗時だけ赤で `未保存・再試行`。
- 未送信変更は localStorage 等に一時保持し、通信復帰・再表示時に再送。画面上の入力値を失わない。
- 原則「保存する」ボタンは不要。残すなら「今すぐ保存」程度の補助ボタンにする。

## データ移植 — 完全照合をしない
1. 現行データを最初に退避する。D1の主要テーブルをSQL/JSON等でスナップショット化。可能ならGoogle側の主要データも1回だけ退避。
2. V3 D1へ一括投入。現在のD1ミラーを主な移植元としてよい。
3. 行数・主要生徒数・主要テーブル件数の大まかな一致だけ確認。全セル比較や全生徒画面比較は禁止。
4. 切替直前に必要なら差分を1回だけ再取込。完全一致に固執しない。
5. 旧本番は削除せず、緊急時に戻れるURL/Versionを残す。

## テスト対象 — 1320 / kase
本番切替前の操作確認はこのダミーを中心に行う。

最低限、次だけ確認：
1. ログインできる。
2. 進捗のチェック/回/LCT/学習日などを数回変更し、自動保存され、再読込後も残る。
3. 宿題を変更し、再読込後も残る。
4. 目標範囲を変更し、再読込後も残る。
5. 連続で数項目を変更しても最後の状態が残る。
6. 通信失敗を1回だけ模擬し、未保存表示→再送で回復する。

**これ以上の大規模なE2E・全生徒巡回・長時間耐久・全件差分照合は今回の切替条件にしない。**

## 性能の受入条件
- 画面操作自体は即時反映。
- 通常の単一保存は、1320での連続確認で概ね2秒以内に `保存済み` になることを目標。
- 保存APIは処理時間をレスポンスヘッダまたはJSONに残し、遅い場合に `D1 / auth / network` のどこかだけ分かる程度でよい。
- Google同期や全画面再読込を保存レスポンスに含めない。

## 実装で避けること
- 現行の複雑な `PRODUCTION_WRITE_APPROVED` / `TEST_WRITE_APPROVED` / mirror / dual-write / 1分cron をそのままV3へコピーしない。
- 保存ごとにGoogleへ往復しない。
- 保存後に全生徒・全教材を再取得しない。
- 「安全のため」と称して新旧全件を何度も比較しない。
- UIの全面デザイン変更を同時にしない。
- 無関係なリファクタリングをしない。

## 切替
1. V3 staging URLで1320確認。
2. 問題なければ現行データを最終投入。
3. 本番URLまたはRegistryのリンクをV3へ切替。
4. 旧Worker/旧D1は即削除しない。
5. 切替後、実生徒2〜3名を読むだけの軽い確認をして終了。

## 成果物
- V3 Workerコード
- D1 schema / migration
- 旧→V3データ移植スクリプトまたは手順
- 1320 smoke test
- staging URL
- 本番切替手順とロールバック先
- READMEに「D1が正本、Googleは通常保存経路ではない」と明記

## 最重要
最初に長い監査レポートを作らないこと。

**コード作成 → staging構築 → 1320確認 → データ移植 → 切替**の順で進めること。

今回の優先順位は「完全性」よりも、保存経路の単純化・速さ・再読込後も残ること。
