type Row=Record<string,unknown>;type SmokeEnv=Env&{SMOKE_TOKEN:string};
const H={"content-type":"application/json; charset=utf-8","cache-control":"no-store"};const json=(v:unknown,s=200)=>new Response(JSON.stringify(v),{status:s,headers:H});const text=(v:unknown)=>String(v??"").trim();const isRow=(v:unknown):v is Row=>typeof v==="object"&&v!==null&&!Array.isArray(v);const auth=(q:Request,e:SmokeEnv)=>Boolean(e.SMOKE_TOKEN)&&((q.headers.get("authorization")||"").replace(/^Bearer\s+/i,""))===e.SMOKE_TOKEN;const hash=async(token:string)=>{const d=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(token));return Array.from(new Uint8Array(d),b=>b.toString(16).padStart(2,'0')).join('')};
const verify=async(e:SmokeEnv)=>{const samples=[['1100','田中彗翔',121,70],['1097','上野心美',55,112],['1214','渡辺悠一郎',181,288],['1276','元永祐輔',293,581],['1312','長谷川乃夏',133,420]] as const;const results:Row[]=[];for(const [id,name,expectedDone,expectedTarget] of samples){const token=`verify-${id}-${crypto.randomUUID()}-${crypto.randomUUID()}`;const expires=new Date(Date.now()+10*60*1000).toISOString();await e.DB.prepare(`INSERT INTO v3_sessions(token_hash,user_id,role,profile_json,expires_at,created_at,last_seen_at) VALUES(?,?,'STUDENT',?,datetime(?),datetime('now'),datetime('now')) ON CONFLICT(token_hash) DO UPDATE SET expires_at=excluded.expires_at,last_seen_at=datetime('now')`).bind(await hash(token),id,JSON.stringify({studentId:id,id,name}),expires).run();const res=await fetch('https://step-progress-api.stepkobetsu.workers.dev/api',{method:'POST',headers:{'content-type':'text/plain;charset=utf-8'},body:JSON.stringify({action:'getStudentDashboard',token}),redirect:'follow'});const data:unknown=await res.json().catch(()=>null);const row=isRow(data)&&isRow(data.data)&&isRow(data.data.overall)?data.data.overall:null;results.push({studentId:id,name,http:res.status,raw:isRow(data)?data:null,completedCount:row?Number(row.completedCount):null,targetCount:row?Number(row.targetCount):null,expectedCompleted:expectedDone,expectedTarget,match:!!row&&Number(row.completedCount)===expectedDone&&Number(row.targetCount)===expectedTarget});}return json({ok:true,overrides:[],snapshot:results})};export default{async fetch(q:Request,e:SmokeEnv){if(!auth(q,e))return json({error:'NOT_FOUND'},404);if(q.method!=='POST')return json({error:'METHOD_NOT_ALLOWED'},405);const v:unknown=await q.json().catch(()=>null);if(!isRow(v))return json({error:'INVALID_JSON'},400);if(text(v.action)==='inspectRecent1320')return verify(e);return json({error:'NOT_FOUND'},404)}} satisfies ExportedHandler<SmokeEnv>;
