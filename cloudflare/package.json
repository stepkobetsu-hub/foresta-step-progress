{
  "name": "step-progress-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate:local": "wrangler d1 migrations apply step-progress-db --local",
    "d1:migrate:remote": "wrangler d1 migrations apply step-progress-db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260803.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.28.0"
  }
}
